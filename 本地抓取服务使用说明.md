# 本地抓取服务使用说明

## 架构口径

- 本地抓取服务负责运行 Playwright 抓取。
- manager 正式入口运行在 Cloud Run:
  - `https://baboontalkies.pandada.world`
  - `https://baboontalkies-manager-627990150052.asia-east1.run.app`
- 旧入口 `http://fc.pandada.world/baboontalkies_manager` 已废弃，不再用于联调。

## 本地抓取服务

默认本地服务:

```bash
http://localhost:18186
```

常见接口:

```bash
curl http://localhost:18186/health
curl http://localhost:18186/status
curl -X POST http://localhost:18186/trigger-scrape
```

## manager 侧触发方式

如果要通过 manager 去触发远程抓取，请使用正式域名:

```bash
curl -X POST https://baboontalkies.pandada.world/api/trigger-remote-scrape
```

查询最近刷新时间:

```bash
curl -s https://baboontalkies.pandada.world/api/last-refresh-time
```

## 环境变量

- `REMOTE_SCRAPER_URL`
  manager 用它去调用抓取服务
- 默认值:
  `https://s4.s100.vip:3868/trigger-scrape`

## 排查顺序

1. 先确认 Cloud Run manager 健康:
   `curl -s https://baboontalkies.pandada.world/health`
2. 再确认本地抓取服务健康:
   `curl http://localhost:18186/health`
3. 最后再查远程触发是否打通:
   `curl -X POST https://baboontalkies.pandada.world/api/trigger-remote-scrape`
