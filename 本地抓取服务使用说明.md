# 本地抓取服务使用说明

## 架构说明

### 双服务架构
- **本地台式机**：运行抓取服务（Playwright + 定时任务）
- **阿里云函数**：提供 Dashboard 前端访问（读取数据库）

### 数据流程
1. 本地服务每10分钟自动抓取约课宝数据
2. 抓取的数据保存到阿里云 RDS 数据库
3. 云函数 Dashboard 从数据库读取数据展示
4. 云函数 Dashboard 可通过 HTTP 触发本地服务手动抓取

---

## 一、本地抓取服务配置

### 1. 环境变量设置

在终端执行：
```bash
export YUEKEBAO_EMAIL="3kkg7a7k4d66@qq.com"
export YUEKEBAO_PASSWORD="flyegg"
export SCRAPER_PORT=18186  # 可选，默认就是 18186
```

**永久设置**（推荐）：
在 `~/.zshrc` 或 `~/.bashrc` 中添加：
```bash
export YUEKEBAO_EMAIL="3kkg7a7k4d66@qq.com"
export YUEKEBAO_PASSWORD="flyegg"
export SCRAPER_PORT=18186
```

然后执行 `source ~/.zshrc` 使其生效。

### 2. 启动本地抓取服务

```bash
cd /Users/panda/Documents/Apps/BaboonTalkies/baboontalkies_manager
npm run scraper
```

**服务功能：**
- ✅ 启动时立即执行一次抓取
- ✅ 每10分钟自动抓取一次
- ✅ 提供 HTTP API 接口（端口 18186）
- ✅ 自动保存数据到阿里云 RDS 数据库

**日志输出示例：**
```
🚀 本地抓取服务启动中...
📍 API 服务端口: 18186
⏱️  抓取间隔: 10分钟
📧 登录账号: 3kkg7a7k4d66@qq.com
✅ API 服务已启动: http://localhost:18186
```

---

## 二、本地服务 API 接口

### 1. 健康检查
```bash
curl http://localhost:18186/health
```

### 2. 查看状态
```bash
curl http://localhost:18186/status
```

**返回示例：**
```json
{
  "status": "running",
  "isScrapingNow": false,
  "lastRunTime": "2025-11-07T06:55:07.000Z",
  "lastRunStatus": "success",
  "nextRunTime": "2025-11-07T07:05:07.000Z",
  "currentTime": "2025-11-07T06:56:00.000Z",
  "intervalMinutes": 10
}
```

### 3. 手动触发抓取
```bash
# POST 方式
curl -X POST http://localhost:18186/trigger-scrape

# GET 方式（浏览器访问）
open http://localhost:18186/trigger-scrape
```

---

## 三、内网穿透配置

### 目标
让阿里云函数能通过公网访问本地 18186 端口。

### 映射地址
- **本地地址**: `http://localhost:18186`
- **公网地址**: `https://s4.s100.vip:3868`

### 使用 frp 内网穿透（示例）
```ini
[scraper_service]
type = tcp
local_ip = 127.0.0.1
local_port = 18186
remote_port = 3868
```

---

## 四、云函数配置

### 1. 环境变量配置

在 `s.yaml` 中确保配置了远程抓取地址：
```yaml
environmentVariables:
  REMOTE_SCRAPER_URL: "https://s4.s100.vip:3868/trigger-scrape"
```

### 2. 云函数 API 接口

**触发远程抓取：**
```bash
# 通过云函数触发本地抓取
curl -X POST http://fc.pandada.world/baboontalkies_manager/api/trigger-remote-scrape

# 或者 GET 方式
curl http://fc.pandada.world/baboontalkies_manager/api/trigger-remote-scrape
```

### 3. Dashboard 手动触发

访问 Dashboard: `http://fc.pandada.world/baboontalkies_manager`

点击右上角的 **🚀 触发抓取** 按钮即可。

---

## 五、完整工作流程

### 方式一：自动定时抓取（推荐）
1. 本地启动抓取服务：`npm run scraper`
2. 服务每10分钟自动抓取一次
3. 数据自动保存到云数据库
4. Dashboard 自动显示最新数据

### 方式二：手动触发抓取
1. **本地触发**：访问 `http://localhost:18186/trigger-scrape`
2. **云端触发**：在 Dashboard 点击"触发抓取"按钮
3. **API 触发**：调用云函数 `/api/trigger-remote-scrape` 接口

### 方式三：组合使用
- 平时依赖定时抓取
- 需要即时更新时手动触发
- 云函数 Dashboard 始终可访问

---

## 六、常见问题

### 1. 环境变量未设置
**错误信息：**
```
❌ 错误: 未设置环境变量 YUEKEBAO_EMAIL 和 YUEKEBAO_PASSWORD
```

**解决方法：**
```bash
export YUEKEBAO_EMAIL="3kkg7a7k4d66@qq.com"
export YUEKEBAO_PASSWORD="flyegg"
```

### 2. 端口被占用
**错误信息：**
```
Error: listen EADDRINUSE: address already in use :::18186
```

**解决方法：**
```bash
# 查找占用进程
lsof -i :18186

# 杀死进程
kill <PID>

# 或者使用其他端口
export SCRAPER_PORT=18187
npm run scraper
```

### 3. 云函数无法访问本地服务
**检查项：**
- ✅ 本地服务是否运行：`curl http://localhost:18186/health`
- ✅ 内网穿透是否正常：`curl https://s4.s100.vip:3868/health`
- ✅ 云函数环境变量 `REMOTE_SCRAPER_URL` 是否正确

### 4. Playwright 浏览器未安装
**错误信息：**
```
Error: Executable doesn't exist at ...
```

**解决方法：**
```bash
npx playwright install chromium
```

---

## 七、监控与维护

### 查看本地服务日志
服务会在控制台实时输出日志，包括：
- 抓取开始/完成时间
- 数据库保存状态
- 错误信息

### 查看云函数日志
```bash
# 实时日志
s logs -t

# 最近50条
s logs --tail -n 50
```

### 重启服务
```bash
# 按 Ctrl+C 停止服务
# 然后重新启动
npm run scraper
```

---

## 八、快速命令参考

```bash
# 启动本地抓取服务
npm run scraper

# 查看本地服务状态
curl http://localhost:18186/status

# 手动触发抓取
curl -X POST http://localhost:18186/trigger-scrape

# 通过云函数触发
curl -X POST http://fc.pandada.world/baboontalkies_manager/api/trigger-remote-scrape

# 访问 Dashboard
open http://fc.pandada.world/baboontalkies_manager
```

---

## 九、技术栈

- **Playwright**: 浏览器自动化（登录、抓取）
- **Express**: HTTP 服务器
- **MySQL**: 阿里云 RDS 数据库
- **Node.js**: 运行环境
- **Serverless Devs**: 云函数部署工具
