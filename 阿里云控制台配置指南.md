# 阿里云函数计算控制台配置指南

## 当前配置问题

从截图发现的问题:
- ❌ **运行环境**: Python 3.10 → 应该是**自定义运行时**
- ❌ **启动命令**: 空 → 必须填写
- ⚠️ **超时时间**: 60秒 → 太短,建议600-900秒

---

## 修改步骤

### 第1步: 修改运行环境

1. 在函数配置页面,找到 **"运行环境"** 或 **"Runtime"**
2. 点击下拉菜单
3. 选择 **"自定义运行时 (Custom Runtime)"**
   - 如果是 FC 3.0,可能叫 "Custom Runtime"
   - 如果是 FC 2.0,可能叫 "Custom"
4. 系统镜像选择: **Debian 10** 或 **Debian 11**

### 第2步: 配置启动命令

找到 **"启动命令"** 或 **"启动配置"** 区域:

#### 方式A: 使用 bootstrap 脚本 (推荐)

```
启动命令: /code/bootstrap
```

或

```
启动命令: bash /code/bootstrap
```

#### 方式B: 直接启动 Node.js

```
启动命令: node /code/index.mjs
```

> 💡 **说明**: `/code` 是阿里云函数计算的代码目录

### 第3步: 调整超时时间

1. 找到 **"执行时长"** 或 **"超时时间"** 设置
2. 将 `60` 秒改为:
   - **推荐**: `600` 秒 (10分钟)
   - **最佳**: `900` 秒 (15分钟)

> ⚠️ Playwright 启动浏览器需要时间,爬虫操作也较慢

### 第4步: 检查其他配置

确认以下配置:

| 配置项 | 建议值 | 说明 |
|--------|--------|------|
| 内存规格 | 2048 MB | Playwright 需要较多内存 |
| 磁盘空间 | 512 MB 或以上 | 浏览器缓存需要空间 |
| 监听端口 | 9000 | 已正确配置 ✅ |
| 实例并发度 | 1 | 避免资源竞争 |

### 第5步: 配置环境变量

在 **"环境变量"** 区域添加:

```
NODE_ENV=production
PORT=9000
HTTPS=false
```

### 第6步: 上传代码

#### 选项A: 打包上传

在本地项目目录执行:

```bash
# 创建部署包
zip -r deploy.zip \
  src/ \
  ssl/ \
  dashboard.html \
  index.mjs \
  bootstrap \
  package.json \
  package-lock.json \
  -x "*.git*" "node_modules/*" "*.xlsx" "*.py" "*.DS_Store"
```

然后在控制台:
1. 找到 **"代码"** 或 **"函数代码"** 标签
2. 选择 **"上传 ZIP 包"**
3. 上传 `deploy.zip`

#### 选项B: 使用代码包 (在线编辑)

1. 在控制台选择 **"代码包"** 上传方式
2. 也可以直接在线编辑(文件较少时)

#### 选项C: 使用 OSS

1. 先上传 zip 到 OSS
2. 在函数配置中选择 "从 OSS 导入"

### 第7步: 配置 HTTP 触发器

如果还没配置触发器:

1. 进入 **"触发器"** 标签
2. 点击 **"创建触发器"**
3. 配置:
   ```
   触发器类型: HTTP 触发器
   触发器名称: httpTrigger
   请求方式: GET, POST, PUT, DELETE
   认证方式: 匿名访问 (anonymous)
   ```

---

## 验证配置

配置完成后,点击 **"保存"** 或 **"部署"**

### 测试方法

1. **健康检查**:
   ```
   访问: https://你的函数URL/health
   预期返回: {"status":"ok","timestamp":"..."}
   ```

2. **Dashboard首页**:
   ```
   访问: https://你的函数URL/
   预期: 显示 Dashboard 页面
   ```

3. **查看日志**:
   - 在控制台 → 调用日志
   - 查看是否有启动成功的日志:
     ```
     🚀 正在初始化约课宝服务器...
     🚀 仪表板服务器启动成功！(HTTP)
     🌐 访问地址: http://localhost:9000
     ```

---

## 常见错误排查

### 错误1: "no such file or directory"

**原因**: 启动命令路径错误
**解决**:
- 检查启动命令是否为 `/code/bootstrap` 或 `node /code/index.mjs`
- 确保上传的 zip 包含 `bootstrap` 和 `index.mjs`

### 错误2: "command not found: node"

**原因**: 运行环境不是自定义运行时
**解决**: 将运行环境改为 "Custom Runtime"

### 错误3: "Cannot find module"

**原因**: npm 依赖未安装
**解决**:
- 确保 `bootstrap` 脚本包含 `npm install` 命令
- 或在本地先运行 `npm install`,然后把 `node_modules` 一起打包上传(不推荐,包会很大)

### 错误4: "Playwright executable doesn't exist"

**原因**: Playwright 浏览器未安装
**解决**:
- 使用 FunctionAI 的 Playwright 预装环境
- 或在 bootstrap 中添加: `playwright install chromium`

### 错误5: 超时错误

**原因**: 执行时长设置太短
**解决**: 将超时时间改为 600-900 秒

---

## 完整配置清单

最终配置应该是:

```yaml
服务名称: baboontalkies_manager
运行环境: Custom Runtime (自定义运行时)
系统镜像: Debian 10
启动命令: /code/bootstrap 或 node /code/index.mjs
内存: 2048 MB
超时: 600-900 秒
监听端口: 9000
环境变量:
  NODE_ENV: production
  PORT: 9000
  HTTPS: false
触发器: HTTP (匿名访问)
```

---

## 使用 Serverless Devs 自动配置

如果觉得手动配置太麻烦,可以使用自动部署:

```bash
# 1. 安装工具
npm install -g @serverless-devs/s

# 2. 配置密钥
s config add

# 3. 自动部署
s deploy
```

这样会自动应用 `s.yaml` 中的所有配置,无需手动修改!

---

## 需要帮助?

如果遇到问题:
1. 查看函数日志: 控制台 → 调用日志
2. 查看错误信息: 通常会显示具体的错误原因
3. 检查上传的文件: 确保 `index.mjs` 和 `bootstrap` 都存在
4. 测试本地运行: `node index.mjs` 确认代码无误
