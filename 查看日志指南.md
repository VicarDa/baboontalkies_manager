# 阿里云函数计算日志查看指南

## 日志配置信息

- **区域 (Region)**: cn-hangzhou (华东1 杭州)
- **函数名称**: `baboontalkies_manager-mcp`
- **日志项目 (Project)**: `serverless-cn-hangzhou-ac3ea976-494e-52ba-83d4-ba85d6f01d38`
- **日志库 (Logstore)**: `default-logs`

## 在阿里云控制台查看日志

### 方法 1: 通过函数计算控制台 (推荐)

1. **登录阿里云控制台**
   - 访问: https://fc.console.aliyun.com/

2. **选择区域**
   - 在页面左上角选择: **华东1 (杭州)**

3. **找到函数**
   - 在函数列表中找到: **`baboontalkies_manager-mcp`**
   - 点击函数名称进入详情页

4. **查看日志**
   - 点击顶部的 **"日志"** 标签
   - 或点击 **"调用日志"** 标签

5. **筛选日志**
   - 可以设置时间范围
   - 可以搜索关键字 (如 "⏰ 定时触发器", "错误", "成功")

### 方法 2: 通过日志服务控制台

1. **登录日志服务控制台**
   - 访问: https://sls.console.aliyun.com/

2. **选择区域**
   - 选择: **华东1 (杭州)**

3. **找到日志项目**
   - 在项目列表中找到:
   ```
   serverless-cn-hangzhou-ac3ea976-494e-52ba-83d4-ba85d6f01d38
   ```
   - 点击项目名称

4. **进入日志库**
   - 找到并点击: **`default-logs`**

5. **查询日志**
   - 点击 **"查询"** 或 **"实时查询"**
   - 可以使用查询语句,例如:
     ```
     # 查看所有日志
     *

     # 查看定时触发器日志
     * | where content like '%定时触发器%'

     # 查看错误日志
     * | where content like '%错误%' or content like '%失败%'

     # 查看路径重写日志
     * | where content like '%路径重写%'
     ```

## 通过命令行查看日志

### 使用 Serverless Devs

```bash
# 查看最近的日志
s logs --tail -n 50

# 实时查看日志
s logs -t

# 查看最近100条日志
s logs --tail -n 100

# 查看特定时间段的日志
s logs --start-time "2025-11-06 10:00:00" --end-time "2025-11-06 11:00:00"
```

### 如果 s logs 命令卡住

可以直接使用阿里云 CLI:

```bash
# 安装阿里云CLI (如果还没安装)
brew install aliyun-cli  # macOS
# 或访问: https://help.aliyun.com/document_detail/110244.html

# 配置
aliyun configure

# 查询日志
aliyun sls GetLogs \
  --project=serverless-cn-hangzhou-ac3ea976-494e-52ba-83d4-ba85d6f01d38 \
  --logstore=default-logs \
  --from=$(date -u -d '10 minutes ago' +%s) \
  --to=$(date +%s) \
  --query="*"
```

## 常见问题

### Q1: 在控制台找不到函数

**A**: 请确认:
1. 区域是否选择了 **华东1 (杭州)**
2. 函数名称是 **`baboontalkies_manager-mcp`** (不是 `baboontalkies_manager`)
3. 如果仍然找不到,可能在其他区域,检查所有区域

### Q2: 日志标签页显示空白

**A**: 可能的原因:
1. **日志还没有生成** - 访问一下函数触发日志生成:
   ```bash
   curl http://fc.pandada.world/baboontalkies_manager/health
   ```

2. **时间范围设置不对** - 调整时间范围到最近1小时或今天

3. **浏览器问题** - 尝试:
   - 刷新页面 (F5)
   - 清除浏览器缓存
   - 换一个浏览器
   - 使用无痕模式

4. **权限问题** - 确认账号有日志服务的读取权限

### Q3: 显示 "日志服务未开通" 或 "没有权限"

**A**:
1. 在日志服务控制台开通日志服务
2. 给你的账号添加权限: `AliyunLogReadOnlyAccess`

### Q4: s logs 命令卡住不动

**A**:
1. 按 Ctrl+C 终止命令
2. 直接在阿里云控制台查看
3. 或使用阿里云 CLI

## 日志示例

正常运行时你应该看到这些日志:

```
🚀 开始启动约课宝管理系统...
📦 Node.js 版本: v20.11.0
📁 应用基础路径: /baboontalkies_manager
🚀 仪表板服务器启动成功！(HTTP)
🌐 访问地址: http://localhost:9000
```

**定时触发器执行:**
```
⏰ 定时触发器触发 - 开始自动抓取数据
📋 触发时间: 2025-11-06T02:50:00Z
✅ 定时抓取完成
```

**HTTP 请求:**
```
📝 路径重写: /baboontalkies_manager/health → /health
📝 路径重写: /baboontalkies_manager/api/dashboard-data → /api/dashboard-data
📊 开始获取仪表板数据...
```

## 快速访问链接

### 函数计算控制台
```
https://fc.console.aliyun.com/fc/service/cn-hangzhou/function/baboontalkies_manager-mcp/invocations
```

### 日志服务控制台
```
https://sls.console.aliyun.com/lognext/project/serverless-cn-hangzhou-ac3ea976-494e-52ba-83d4-ba85d6f01d38/logsearch/default-logs
```

## 触发日志生成

如果日志为空,先触发一些请求:

```bash
# 健康检查
curl http://fc.pandada.world/baboontalkies_manager/health

# 获取数据
curl http://fc.pandada.world/baboontalkies_manager/api/dashboard-data

# 查看配置
curl http://fc.pandada.world/baboontalkies_manager/api/config
```

然后刷新日志页面,应该就能看到日志了。

## 总结

**在阿里云控制台查看日志的正确步骤:**

1. 访问: https://fc.console.aliyun.com/
2. 选择区域: **华东1 (杭州)**
3. 找到函数: **`baboontalkies_manager-mcp`**
4. 点击 **"日志"** 标签
5. 设置时间范围为 **最近1小时**
6. 查看日志

如果仍然看不到日志,请:
1. 先访问一次函数触发日志: `curl http://fc.pandada.world/baboontalkies_manager/health`
2. 等待几秒钟
3. 刷新日志页面
