# 查看日志指南

## 当前正式环境

- GCP Project ID: `project-59ee4a6b-1c4d-4d7b-a37`
- Cloud Run 服务: `baboontalkies-manager`
- 区域: `asia-east1`
- 正式域名: `https://baboontalkies.pandada.world`

## 优先查看的日志

当前线上问题优先看 Cloud Run 日志，不要先看历史阿里云函数日志。

### gcloud 查看

```bash
gcloud config set project project-59ee4a6b-1c4d-4d7b-a37
gcloud run services logs read baboontalkies-manager --region asia-east1 --limit 200
```

### GCP 控制台查看

1. 打开 Cloud Run
2. 选择 `baboontalkies-manager`
3. 进入 `Logs`
4. 或在 Logs Explorer 中按服务名过滤

## 常用线上检查

```bash
curl -s https://baboontalkies.pandada.world/health
curl -s https://baboontalkies.pandada.world/api/last-refresh-time
curl -X POST https://baboontalkies.pandada.world/api/refresh-data
```

## 历史入口说明

- `http://fc.pandada.world/baboontalkies_manager` 是历史阿里云入口。
- 只有在明确排查 legacy 环境时，才需要再看阿里云相关日志。
- 日常刷新数据、线上验证、故障排查都应以 Cloud Run 为准。
