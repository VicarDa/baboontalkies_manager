# 自动抓取配置说明

## 配置概览

已配置定时触发器,每10分钟自动抓取一次约课宝数据。

## 执行时间表

**每小时执行 6 次:**
- 每小时的 00 分 (如: 01:00, 02:00, 03:00...)
- 每小时的 10 分 (如: 01:10, 02:10, 03:10...)
- 每小时的 20 分 (如: 01:20, 02:20, 03:20...)
- 每小时的 30 分 (如: 01:30, 02:30, 03:30...)
- 每小时的 40 分 (如: 01:40, 02:40, 03:40...)
- 每小时的 50 分 (如: 01:50, 02:50, 03:50...)

**每天执行次数:** 24小时 × 6次/小时 = **144次/天**

## Cron 表达式说明

```yaml
cronExpression: "0 0,10,20,30,40,50 * * * *"
```

**格式:** `秒 分 时 日 月 星期`

- `0` - 每次在第0秒执行
- `0,10,20,30,40,50` - 在第0、10、20、30、40、50分钟
- `*` - 每小时
- `*` - 每天
- `*` - 每月
- `*` - 每周

## 触发器配置

```yaml
triggers:
  - triggerName: autoScraper
    triggerType: timer
    description: "定时触发器 - 每10分钟自动抓取数据"
    qualifier: LATEST
    triggerConfig:
      cronExpression: "0 0,10,20,30,40,50 * * * *"
      enable: true
      payload: '{"action":"auto-scrape","interval":"10min"}'
```

## 工作原理

1. **定时触发:** 阿里云定时触发器按 Cron 表达式自动触发函数

2. **触发识别:** `index.mjs` 检测到 `req.triggerName === 'autoScraper'`

3. **执行抓取:** 调用 `serverInstance.scrapeYuekebaoCourses()` 方法

4. **数据保存:**
   - 抓取课程数据和会员卡数据
   - 保存到 MySQL 数据库
   - 生成 Excel 文件

5. **日志记录:** 在函数日志中记录执行结果

## 查看执行情况

### 方法 1: 查看函数日志

```bash
# 查看最近 50 条日志
s logs --tail -n 50

# 实时查看日志
s logs -t
```

**关键日志标识:**
```
⏰ 定时触发器触发 - 开始自动抓取数据
📋 触发时间: 2025-11-06T02:40:00Z
✅ 定时抓取完成
```

### 方法 2: 阿里云控制台

1. 登录阿里云函数计算控制台
2. 进入函数 `baboontalkies_manager-mcp`
3. 点击"日志"标签
4. 筛选包含"定时触发器"的日志

### 方法 3: 查看触发器调用记录

1. 函数计算控制台 → 函数详情
2. "触发器"标签
3. 点击 `autoScraper` 触发器
4. 查看调用历史

## 暂停/启用自动抓取

### 暂停自动抓取

**方法 1: 修改 s.yaml**

```yaml
triggerConfig:
  cronExpression: "0 0,10,20,30,40,50 * * * *"
  enable: false  # 改为 false
```

然后部署:
```bash
s deploy -y
```

**方法 2: 控制台操作**

1. 函数计算控制台 → 触发器
2. 找到 `autoScraper` 触发器
3. 点击"禁用"

### 启用自动抓取

将 `enable` 改为 `true` 并重新部署,或在控制台点击"启用"。

## 调整执行频率

### 改为每 5 分钟执行一次

```yaml
cronExpression: "0 */5 * * * *"
```

执行时间: 00, 05, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55 分

### 改为每 15 分钟执行一次

```yaml
cronExpression: "0 0,15,30,45 * * * *"
```

执行时间: 00, 15, 30, 45 分

### 改为每 30 分钟执行一次

```yaml
cronExpression: "0 0,30 * * * *"
```

执行时间: 00, 30 分

### 改为每小时执行一次

```yaml
cronExpression: "0 0 * * * *"
```

执行时间: 每小时的00分

### 只在工作时间执行 (9:00-18:00)

```yaml
cronExpression: "0 0,10,20,30,40,50 9-18 * * *"
```

执行时间: 9:00-18:00 之间,每10分钟一次

### 只在工作日执行

```yaml
cronExpression: "0 0,10,20,30,40,50 * * * MON-FRI"
```

执行时间: 周一至周五,每10分钟一次

## 手动触发测试

### 方法 1: 通过 Dashboard 手动刷新

访问 Dashboard 页面,点击"刷新数据"按钮。

### 方法 2: 控制台手动触发

1. 函数计算控制台 → 触发器
2. 找到 `autoScraper` 触发器
3. 点击"触发一次"

### 方法 3: 测试函数

1. 函数计算控制台 → 测试函数
2. 使用以下测试事件:

```json
{
  "triggerName": "autoScraper",
  "triggerTime": "2025-11-06T10:00:00Z",
  "payload": "{\"action\":\"auto-scrape\",\"interval\":\"10min\"}"
}
```

3. 点击"调用"

## 成本估算

**函数调用次数:**
- 每天 144 次自动抓取
- 每月约 4,320 次 (按 30 天计算)

**预计费用:**
- 函数计算按调用次数和执行时长收费
- 建议查看阿里云函数计算定价页面

**优化建议:**
- 如果不需要这么频繁,可以调整为每30分钟或每小时执行一次
- 可以设置只在工作时间执行,减少不必要的调用

## 监控和告警

### 设置云监控告警

1. 云监控控制台 → 应用分组
2. 创建告警规则:
   - 函数执行失败次数 > 3 次/10分钟
   - 函数执行超时次数 > 1 次/小时
   - 函数执行错误率 > 10%

3. 配置通知方式 (邮件/短信/钉钉)

## 故障排查

### 问题: 定时任务没有执行

**检查项:**
1. 触发器是否启用: `s info | grep -A 30 "triggers:"`
2. 查看日志是否有错误: `s logs --tail -n 50`
3. 检查函数是否在运行: `curl http://fc.pandada.world/baboontalkies_manager/health`

### 问题: 定时任务执行失败

**检查项:**
1. 查看错误日志: `s logs --tail -n 100 | grep "ERROR\|❌"`
2. 检查数据库连接是否正常
3. 检查约课宝账号密码是否正确
4. 检查函数超时时间是否足够 (当前: 900秒)

### 问题: 数据抓取不完整

**检查项:**
1. 查看日志中的数据统计
2. 检查约课宝网站是否有变化
3. 检查网络连接是否稳定
4. 检查 Playwright 浏览器是否正常启动

## 部署更新

修改配置后需要重新部署:

```bash
# 部署所有配置(包括触发器)
s deploy -y

# 查看部署结果
s info

# 验证触发器配置
s info | grep -A 30 "triggers:"
```

## 注意事项

1. **单实例并发:** 配置为 `instanceConcurrency: 1`,确保同一时间只有一个抓取任务在执行

2. **执行超时:** 抓取任务可能需要较长时间,已设置 `timeout: 900` (15分钟)

3. **数据一致性:** 每次抓取会更新数据库,确保始终显示最新数据

4. **费用控制:** 每 10 分钟执行一次,请根据实际需求调整频率

5. **错误重试:** 如果抓取失败,下次触发时会自动重试

## 当前配置总结

- ✅ HTTP 触发器: 用于 Web 访问和手动刷新
- ✅ 定时触发器: 每 10 分钟自动抓取一次
- ✅ 日志服务: 已启用,可查看详细执行日志
- ✅ 自定义域名: http://fc.pandada.world/baboontalkies_manager

系统现在会自动保持数据最新状态! 🎉
