# Serverless Devs 部署完整步骤

## 准备工作

### 1. 获取阿里云 AccessKey

访问: https://ram.console.aliyun.com/manage/ak

或者:
1. 登录阿里云控制台
2. 点击右上角头像
3. 选择 "AccessKey 管理"
4. 点击 "创建 AccessKey"
5. **立即保存** AccessKey ID 和 AccessKey Secret (Secret只显示一次!)

需要记录的信息:
```
AccountID: ____________ (12位数字,在账号管理中查看)
AccessKey ID: LTAI5t____________
AccessKey Secret: ______________ (保密!)
```

---

## 部署步骤

### 步骤1: 安装 Serverless Devs

```bash
npm install -g @serverless-devs/s
```

等待安装完成...

### 步骤2: 验证安装

```bash
s --version
```

应该显示版本号,如: `@serverless-devs/s: 3.x.x`

### 步骤3: 配置阿里云密钥

```bash
s config add
```

**按提示输入**:

```
? Please select a provider:
  → 选择 "Alibaba Cloud (alibaba)"
  → 按回车

? AccountID:
  → 输入你的12位账号ID
  → 按回车

? AccessKeyID:
  → 粘贴你的 AccessKey ID
  → 按回车

? AccessKeySecret:
  → 粘贴你的 AccessKey Secret
  → 按回车

? Please create alias for key pair:
  → 输入 "default" 或直接按回车
  → 按回车
```

看到 `✔ Configuration saved successfully` 表示成功!

### 步骤4: 验证配置

```bash
s config get -a default
```

应该显示你刚配置的信息(Secret会隐藏)

### 步骤5: 部署到阿里云

```bash
cd /Users/panda/Documents/Apps/BaboonTalkies/baboontalkies_manager
s deploy
```

部署过程会显示:
```
[2024-xx-xx xx:xx:xx] [INFO] [FC3] - Checking Service baboontalkies_manager exists
[2024-xx-xx xx:xx:xx] [INFO] [FC3] - Updating function...
[2024-xx-xx xx:xx:xx] [INFO] [FC3] - Uploading code...
...
✔ Deploy completed

Function URL: https://xxxxx.cn-hangzhou.fcapp.run
```

### 步骤6: 测试访问

部署完成后,复制显示的 Function URL,在浏览器访问:

```
# 健康检查
https://你的函数URL/health

# Dashboard首页
https://你的函数URL/

# API接口
https://你的函数URL/api/dashboard-data
```

---

## 常见问题

### Q1: 提示 "command not found: s"

**原因**: Serverless Devs 未安装成功
**解决**:
```bash
# 重新安装
npm install -g @serverless-devs/s

# 检查 npm 全局路径
npm config get prefix

# 确保该路径在 PATH 中
echo $PATH
```

### Q2: 提示 "The config default does not exist"

**原因**: 配置未成功
**解决**: 重新执行 `s config add`

### Q3: 部署时提示权限错误

**原因**: AccessKey 没有足够权限
**解决**:
1. 登录阿里云控制台
2. RAM 访问控制 → 用户
3. 给用户添加 "AliyunFCFullAccess" 权限

### Q4: 部署很慢或卡住

**原因**: 上传代码包较大
**解决**:
- 确保没有打包 node_modules (会自动安装)
- 排除不必要的文件(.git, *.xlsx 等)

### Q5: 部署后访问 404

**原因**: HTTP 触发器未配置
**解决**:
- 检查 s.yaml 中的 triggers 配置
- 或在控制台手动添加 HTTP 触发器

---

## 更新代码

修改代码后重新部署:

```bash
# 完整部署(代码+配置)
s deploy

# 仅部署代码(更快)
s deploy --type code

# 部署前预览变更
s deploy --dry-run
```

---

## 查看日志

```bash
# 实时查看日志
s logs -t

# 查看最近的日志
s logs

# 查看指定时间范围
s logs --start-time "2024-01-01 00:00:00" --end-time "2024-01-01 23:59:59"
```

---

## 其他有用命令

```bash
# 查看函数信息
s info

# 调用函数测试
s invoke

# 删除函数
s remove

# 查看帮助
s deploy --help
```

---

## 完整命令汇总

```bash
# 1. 安装工具
npm install -g @serverless-devs/s

# 2. 配置密钥
s config add

# 3. 进入项目目录
cd /Users/panda/Documents/Apps/BaboonTalkies/baboontalkies_manager

# 4. 部署
s deploy

# 5. 查看日志
s logs -t
```

---

## 需要帮助?

- Serverless Devs 文档: https://www.serverless-devs.com/
- 阿里云 FC 文档: https://help.aliyun.com/product/50980.html
- GitHub Issues: https://github.com/Serverless-Devs/Serverless-Devs/issues
